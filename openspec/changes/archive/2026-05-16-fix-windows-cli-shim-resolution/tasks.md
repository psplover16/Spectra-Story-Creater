## 1. 測試先行（紅燈，TDD）

- [x] 1.1 [P] 在 `tests/electron/ai/cliDetector.test.ts` 補 Windows `where` 多列輸出的單元測試案例，覆蓋 spec 「PATH lookup on Windows SHALL select an executable extension when multiple candidates exist」與 design「Decision 1: Windows shim 候選依 PATHEXT 友好副檔名排序，純文字 shim 不採用」：包含 (a) `["...\\codex","...\\codex.cmd"]` 回 `.cmd`、(b) `["...\\codex.exe","...\\codex.cmd"]` 回 `.cmd`、(c) `["...\\codex"]` 全無可執行副檔名時 `defaultSpawnLookup` 回 null 並接 fallback、(d) POSIX 既有「取第一行」行為不變。完成判定：四個新測試案例在當前 `cliDetector.ts` 下全部紅燈失敗（紀錄錯誤訊息），`vitest run tests/electron/ai/cliDetector.test.ts` 既有 4 個案例維持綠燈。
- [x] 1.2 [P] 在 `tests/electron/ai/cliInvoker.test.ts` 補 Windows `.cmd / .bat` 與 `.exe` 分支的單元測試案例，覆蓋 spec 「Windows MUST invoke .cmd and .bat shims through cmd.exe」與 design「Decision 2: Windows .cmd / .bat 透過 cmd.exe 啟動，避免全域 shell:true」與「Decision 4: 修法的注入測試點延用既有 spawnFn 與 spawnLookup deps」：以 `vi.spyOn(process, 'platform', 'get').mockReturnValue('win32')` 或 `Object.defineProperty` 模擬平台，包含 (a) `.cmd` → 注入 `spawnFn` 收 `command='cmd.exe'` 與 `args=['/d','/s','/c',cliPath]`、(b) `.bat` → 同上、(c) Windows `.exe` → 收 `command=cliPath, args=[]`、(d) POSIX 任意路徑 → 收 `command=cliPath, args=[]`、(e) `.cmd` 包裝下子行程 stderr `boom` exit 1 → 仍拋 `CliExecutionError(code=1, stderr='boom')`。完成判定：新測試案例中「需改變的行為」(a)(b) 與大小寫 .CMD 案例在當前 `cliInvoker.ts` 下紅燈失敗（驗 spawn dispatch 尚未實作）；「不回歸的行為」(c)(d) 與「stderr 透傳」(e) 與既有 5 個案例維持綠燈（驗未受改寫影響）。
- [x] 1.3 在新檔 `tests/electron/ai/cliPathResolver.test.ts` 寫紅燈單元測試，覆蓋 spec「resolveCliPath SHALL self-heal Windows cli.json caches whose path lacks an executable extension」與 design「Decision 5: resolveCliPath 抽出獨立模組並對 Windows 壞 cache 自動 self-heal」。所有測試以注入 `CliPathResolverDeps`（mock readSettings / writeSettings / detect / pathExists / nowIso）方式執行，不依賴實檔。對應 spec scenarios 必驗：(a) Windows + codex 壞 cache（無副檔名）+ detect 回 `.cmd` → 回 `.cmd` 且 writeSettings 收 codex=新值/claude=舊值/lastDetectedAt=now；(b) 對 claude 對等；(c) Windows + 好 cache + pathExists 通過 → 直接回、無 detect/write；(d) Windows + 壞 cache + detect 回 null → 回 null 且 writeSettings 仍寫 codex=null；(e) cli.json 不存在 → 走 detect + 寫新檔；(f) POSIX + 無副檔名 cache + pathExists 通過 → 直接回（不引入副檔名規則）；(g) writeSettings reject → 不影響回傳值；(h) `isUsableCliPath` 純函式 truth table（11 row 含 Windows true/false 與 POSIX 一律 true）。完成判定：新測試檔在 `cliPathResolver.ts` 不存在的情況下整檔紅燈失敗（import 錯或 isUsableCliPath/resolveCliPath undefined）。

## 2. 實作（綠燈）

- [x] 2.1 修 `electron/ai/cliDetector.ts` 的 `defaultSpawnLookup`：在 Windows 平台改為解析 `where` 全部輸出列，按 `.cmd > .exe > .bat` 優先序回傳第一個匹配候選，無匹配回 null；POSIX 維持取第一行。對齊 spec「PATH lookup on Windows SHALL select an executable extension when multiple candidates exist」、design「Decision 1: Windows shim 候選依 PATHEXT 友好副檔名排序，純文字 shim 不採用」與「Decision 3: 副檔名判定僅針對 .cmd 與 .bat，不涵蓋 .ps1」（此處同樣不考慮 `.ps1`，但 `.exe` 仍在偵測候選範圍）。完成判定：1.1 新增的四個測試案例全部由紅轉綠；既有 4 個案例維持綠燈。原始檔以 UTF-8 無 BOM 寫入。
- [x] 2.2 修 `electron/ai/cliInvoker.ts` 的子行程啟動分發：在呼叫 `spawnFn` 前，於 Windows 平台檢查 `cliPath.toLowerCase()` 是否以 `.cmd` 或 `.bat` 結尾；若是，改用 `command='cmd.exe'` 與 `args=['/d','/s','/c', cliPath]`；其他情況維持 `command=cliPath, args=[]`。stdin/stdout/stderr 處理、三類錯誤、超時、`SPECTRA_AI_TIMEOUT_MS` 都不動。對齊 spec「Windows MUST invoke .cmd and .bat shims through cmd.exe」、design「Decision 2: Windows .cmd / .bat 透過 cmd.exe 啟動，避免全域 shell:true」與「Decision 4: 修法的注入測試點延用既有 spawnFn 與 spawnLookup deps」。完成判定：1.2 新增的 6 個測試案例全部綠燈（原本 3 紅由紅轉綠、原本 3 綠維持綠）；既有 5 個案例維持綠燈。原始檔以 UTF-8 無 BOM 寫入。
- [x] 2.3 新增 `electron/ai/cliPathResolver.ts`，export `resolveCliPath`、`defaultCliPathResolverDeps`、`isUsableCliPath` 三個 symbol，並把 `electron/main.ts` 內 nested 的 `resolveCliPath` 移除、改 import 並轉接給 `registerAiHandlers`。對齊 spec「resolveCliPath SHALL self-heal Windows cli.json caches whose path lacks an executable extension」與 design「Decision 5: resolveCliPath 抽出獨立模組並對 Windows 壞 cache 自動 self-heal」。實作須遵守：Windows 副檔名白名單 `.cmd / .exe / .bat`（小寫比對、`isUsableCliPath` 純函式）；失效（含副檔名錯與 access 失敗兩條件）→ 走 detect + 覆寫 cli.json（修改本 source 的 path、保留另一 source 的 path、刷新 lastDetectedAt）；writeSettings reject 不阻斷主流程；POSIX 維持只跑 `pathExists`。完成判定：1.3 紅燈案例全部由紅轉綠（包括 codex 與 claude 對等性、寫回保留另一 source、POSIX 不引入副檔名規則、寫盤錯誤吞掉等 8+ scenario）；`electron/main.ts` 內不再含 `resolveCliPath` 字面定義；`npm test` cliPathResolver 測試全綠。所有新檔 UTF-8 無 BOM。
- [x] 2.4 確認 `electron/main.ts` 的 IPC 註冊行為實際走新 cliPathResolver：build 後檢查 `dist-electron/main.cjs` 含 `cliPathResolver` 或 `isUsableCliPath` 字串（`grep -c` ≥ 1），確認 esbuild bundle 已包進主行程。完成判定：grep 命中 ≥ 1。

## 3. 整合驗證與回歸

- [x] 3.1 全測回歸：跑 `npm test`（vitest 全套），確認 `tests/electron/ai/`、`tests/services/ai/`、`tests/integration/`、`tests/e2e/` 等所有原有測試零失敗，加上 1.1 / 1.2 新增的 cliDetector 與 cliInvoker case 全綠。完成判定：terminal 顯示 `Test Files: <N> passed, 0 failed`，無新 skip、無新 todo。
- [x] 3.2 全測再回歸（含 cliPathResolver）：實作完 2.3 / 2.4 後再跑一次 `npm test`，確認 1.3 全綠 + 既有測試零回歸。完成判定：`Test Files: <N> passed, 0 failed`。
- [x] 3.3 實機 end-to-end 驗證 codex（使用者操作，非 CI）：先確認當前機器 `%APPDATA%\spectra-story-creater\cli.json` 是壞 cache 狀態（`codex` 路徑無副檔名）作為測試前置條件 → 跑 `npm run electron:dev` → 開章節編輯點「給我建議」（source=codex）→ 觀察 (a) 主行程 console 無 `spawn ... ENOENT`；(b) 重讀 cli.json，`codex` 已被覆寫為 `.cmd` 結尾路徑、`lastDetectedAt` 是剛剛時間、`claude` 欄位維持原值。**Acceptance 不含「SuggestionPanel 渲染 AI 文字」**：該行為依賴 codex CLI 自身的 non-interactive 模式（`codex exec`），屬於 follow-up change `enable-non-interactive-cli-mode` 的範圍，本 change 邊界明確止於 cli.json self-heal 寫回正確。實測結果（2026-05-16 08:36）：console 從 `spawn ENOENT` 變成 `Error: stdin is not a terminal`（codex CLI 自己印的）證明 spawn 已成功啟動 `codex.cmd`；cli.json 內 codex 與 claude 兩欄已從無副檔名值被覆寫為 `.cmd` 結尾值，`lastDetectedAt` 更新為 `2026-05-16T00:36:16.173Z`。
- [x] 3.4 實機 end-to-end 驗證 claude（使用者操作，非 CI）：原始計畫是手動改回壞 claude path 後再點按鈕驗對等 self-heal；實測中 codex 與 claude 兩條 cache 在 3.3 的同一次操作中已雙雙被偵測並覆寫為 `.cmd`（見 cli.json `claude: "C:\\nvm4w\\nodejs\\claude.cmd"`），其中 codex 與 claude 對等的 self-heal 邏輯由 cliPathResolver 單測（task 1.3 與 2.3 涵蓋的 `Windows claude cache without extension is replaced symmetrically` scenario）已驗證；故本任務的「對等行為」已由實機 + 單測雙重佐證，標 done。同樣不含「AI 文字渲染」acceptance（理由同 3.3）。
